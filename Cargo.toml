[package]
name = "test-baremetal-with-stable-rustc"
version = "0.1.0"
edition = "2024"

[dependencies]
bootloader = "0.9.33"

[profile.dev]
panic = "abort"

[profile.release]
lto = true
opt-level = "s"
codegen-units = 1
panic = "abort"
strip = "symbols"
